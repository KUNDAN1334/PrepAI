// components/landing/WordReveal.tsx
'use client';

/**
 * @deprecated Superseded by the framer-motion `WordReveal` in
 * `components/landing/motion-primitives.tsx`, which adds a blur-in per word.
 * Kept as a re-export so any older import keeps working.
 */
export { WordReveal as default, WordReveal } from './motion-primitives';

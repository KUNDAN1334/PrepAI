// components/landing/Reveal.tsx
'use client';

/**
 * @deprecated Superseded by the framer-motion `Reveal` in
 * `components/landing/motion-primitives.tsx`, which supports directional
 * entrances and stagger. Kept as a re-export so any older import keeps working;
 * prefer importing from motion-primitives directly.
 */
export { Reveal as default, Reveal } from './motion-primitives';
